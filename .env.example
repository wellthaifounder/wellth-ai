# ── Frontend (Vite) ───────────────────────────────────────────────────────────
# Get these from your Supabase project dashboard → Settings → API
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_URL="https://your-project-id.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-public-key"

# ── Supabase Edge Functions ───────────────────────────────────────────────────
# Injected automatically by Supabase at runtime — do NOT set these manually:
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY

# ── Stripe ────────────────────────────────────────────────────────────────────
# Publishable key (safe to expose in frontend) — Vercel env var
VITE_STRIPE_PUBLISHABLE_KEY="pk_live_..."
# Secret key — Supabase Edge Function secret only, never in frontend
STRIPE_SECRET_KEY="sk_live_..."

# ── Plaid ─────────────────────────────────────────────────────────────────────
# From Plaid dashboard → Team Settings → Keys
PLAID_CLIENT_ID="your-plaid-client-id"
PLAID_SECRET="your-plaid-secret"
PLAID_ENV="sandbox"  # sandbox | development | production
# 32-byte (256-bit) random key used to encrypt Plaid access tokens at rest
# Generate with: openssl rand -hex 32
PLAID_ENCRYPTION_KEY="your-64-char-hex-encryption-key"

# ── Email (Resend) ────────────────────────────────────────────────────────────
RESEND_API_KEY="re_..."

# ── CORS ─────────────────────────────────────────────────────────────────────
# Restrict edge function responses to this origin (your production domain)
ALLOWED_ORIGIN="https://wellth-ai.app"

# ── Lovable Integration ───────────────────────────────────────────────────────
LOVABLE_API_KEY="your-lovable-api-key"

# ── Feature Flags ─────────────────────────────────────────────────────────────
# Wave 3/4 (2026-05) UX experiments. As of 2026-05-06 these default ON in code
# (no users yet → no telemetry → ship the recommended designs). Set any of these
# to "false" in Vercel to roll back without a code change. See
# docs/ux-review-2026-05.md §3 + §4 + §5 pre-mortem.

# Auto-dismiss the OnboardingWizard carousel for users who picked
# userIntent === 'billing' (HSA / both still see it). Default: on.
VITE_FF_AUTO_DISMISS_ONBOARDING_FOR_BILLING="true"

# Show the Get-Started progress ribbon only on /dashboard (instead of every
# authenticated page). Default: on.
VITE_FF_SCOPE_GET_STARTED_TO_DASHBOARD="true"

# Collapse Bills/Ledger IA: /ledger → /bills?view=ledger; sidebar drops
# standalone Ledger; bottom-tab repoints. Default: on.
VITE_FF_BILLS_LEDGER_IA_COLLAPSE="true"

# Wave 4 — collapse Bills/Ledger IA: /ledger redirects to /bills?view=ledger,
# sidebar drops Ledger, Bills renders both views via a tab switcher.
VITE_FF_BILLS_LEDGER_IA_COLLAPSE="false"
